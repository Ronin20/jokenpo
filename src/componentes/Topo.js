import React from 'react';
import { View, Image } from 'react-native';

export default Topo = () => {
    return(
        <View>
            <Image
				source={require('../../images/jokenpo.png')}
                style={{width: '100%'}}
            />
        </View>
    )
}